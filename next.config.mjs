/** @type {import('next').NextConfig} */
const nextConfig = {
  // Por ahora, no necesitas ninguna configuración específica aquí.
  // Tu proyecto funcionará correctamente con un objeto vacío.
};

export default nextConfig;